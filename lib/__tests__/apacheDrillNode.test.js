const assert = require('assert');
const apacheDrillNode = require('../index.js');

describe('apacheDrillNode', () => {
  it('has a test', () => {
    assert(false, 'apacheDrillNode should have a test');
  });
});
